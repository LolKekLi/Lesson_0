using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CharcterConreoller : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
      // bibib
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
